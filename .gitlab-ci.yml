image: docker:latest

variables:
  DOCKER_HOST: "unix:///var/run/docker.sock"

services:
  - docker:dind

before_script:
  - docker info

variables:
  DOCKER_DRIVER: overlay2

stages:
  - build

build:
  stage: build
  script:
    - docker build -t my-image:latest .
